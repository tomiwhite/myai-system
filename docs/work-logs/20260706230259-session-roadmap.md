# Work Log: Session Roadmap And Start

**Data**: 2026-07-06

## Cel sesji

- Uporzadkowac plan prac na biezaca i kolejne sesje.
- Zaczac od najtanszych zmian o wysokiej wartosci: niespojnosci diagnostyki, twarde sciezki uzytkownika, bezpieczenstwo cleanupow.

## Plan tej sesji

1. Ujednolicic diagnostyke Git config we wszystkich lekkich skryptach sprawdzajacych.
2. Wybrac pierwszy skrypt z twardymi sciezkami `C:\Users\tomiw\...` i przeniesc go na zmienne srodowiskowe.
3. Zweryfikowac zmiany przez uruchomienie najwezszych mozliwych checkow.
4. Zapisac wynik i decyzje do dokumentacji projektu.

## Plan kolejnych sesji

### Sesja 2

- Wyniesc stale sciezki lokalnego profilu do wspolnej konfiguracji opartej o `$env:USERPROFILE`.
- Zaczac od `cleanup-safe-junk.ps1`, `deep-system-audit.ps1`, `reinstall-three-apps.ps1`.

### Sesja 3

- Obnizyc ryzyko cleanupow przez wspolny `-DryRun`, wspolne logowanie i jednolita safeliste.
- Przejrzec `auto-cleanup.ps1`, `auto-cleanup-orphaned.ps1`, `deep-cleanup-admin.ps1`.

### Sesja 4

- Ujednolicic raportowanie: JSON do `reports/`, dokumentacja opisowa do `docs/reports/`.
- Poprawic miejsca, gdzie skrypty wypisuja nieaktualne komendy lub nieistniejace sciezki.

### Sesja 5

- Dolozyc testy operacyjne dla starterow i diagnostyki: minimalne checki CLI, walidacje outputu i preflight zaleznosci.
- Sprawdzic, czy uruchamianie na nowej maszynie nie wymaga recznych zmian sciezek.

## Rozpoczecie prac

- Start od ujednolicenia Git email w `quick-permissions-check.ps1` i `audit-permissions.ps1`.
- Po tym kroku od razu uruchomic szybki check diagnostyczny, zanim rozszerze zakres zmian.